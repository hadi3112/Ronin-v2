/**
 * storageService.js (Placeholder)
 * 
 * This service will handle fetching media from Firebase Cloud Storage.
 * It provides clean insertion points for video preloading and assets.
 */

export async function getVideoUrl(gsPath) {
  // TODO: Implement getDownloadURL() using Firebase Storage SDK
  // e.g. return await getDownloadURL(ref(storage, gsPath));
  console.log(`[Placeholder] Would fetch download URL for: ${gsPath}`);
  return null;
}
